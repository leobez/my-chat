-> REACT/NODE FOR WEB.
-> API WITH DATABASE.
-> JAVA FOR DESKTOP.  

--/--

TODO: 
    -> WHEN FRIEND REQUEST GETS DENIED -> DELETE FROM DATABASE
    -> CREATE DELETE FRIEND ROUTE...
    -> IMPLEMENT ON FRONTEND -> IMPLEMENT VIA WEBSOCKET
        -> ONLINE STATUS
        -> NOTIFICATION
        -> PROFILE PAGE 
        
    ACCOUNT:
    -> FORGOT PASSWORD
    -> GUEST ACCOUNT


MAYBE...
    -> Instead of comparing session_token with req.body, just use session token right away... (remove userIdVerifier)
    -> User more of TokenVerifier (already sets req.data = userData)